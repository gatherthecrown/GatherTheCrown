
[build]
  command = "corepack enable && pnpm install && pnpm --filter @game/client build"
  publish = "packages/client/dist"
  functions = "netlify-functions"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
